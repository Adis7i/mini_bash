#include "../utils/parser.hpp"
#ifndef ADIS_HANDLERS_HPP
#define ADIS_HANDLERS_HPP
using namespace utls;
namespace init {
void cat_init(prsr::Parser& parser);
void lsdir_init(prsr::Parser& parser);
}
#endif
